name: hello-world

on:
  pull_requestes:
    branches: 
        - main

jobs: 
  hello:
    runs-on: ubuntu-latest
    name: my-job
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: hello
        run: echo "The time was"
